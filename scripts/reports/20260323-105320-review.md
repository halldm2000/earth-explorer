# Auto-Task Review: View Mode Switcher UI
Date: 2026-03-23T10:53:20
Branch: view-switcher (worktree)

## Task
Add a view mode switcher UI component to the globe. Create a small toggle group showing three buttons: [1] 2.5D, [2] 2D, [3] 3D — positioned in the bottom-left area near the status bar. The active mode should be highlighted. Clicking a button calls viewer.scene.morphToColumbusView(1.0), morphTo2D(1.0), or morphTo3D(1.0) respectively. When switching to 2D or Columbus view, call hideAllBuildings() from engine.ts. When switching to 3D, call restoreBuildings(). The component should listen for scene mode changes (viewer.scene.morphComplete event) to update the highlighted button if the mode changes from keyboard shortcuts (1/2/3 keys). Style it to match the existing dark UI theme. Add it to App.tsx alongside the existing bottom bar components.

## Changes
| File | Description |
|------|-------------|
| `src/ui/ViewModeSwitcher.tsx` | **New file.** Three-button toggle (2.5D / 2D / 3D) that calls Cesium morph methods and manages building visibility. Syncs state via `morphComplete` event. |
| `src/ui/BottomBar.tsx` | Imports and positions `ViewModeSwitcher` in the bottom bar, to the right of GlobalControls at `left: 60px`. |

No changes were made to `App.tsx` or `engine.ts` — the component was correctly integrated via the existing `BottomBar` composition rather than modifying the app shell directly.

## Scorecard

| Category | Score | Notes |
|----------|-------|-------|
| Completeness | PASS | All 6 success criteria met: 3-button toggle visible, click switches mode, active highlighted, buildings hidden in 2D/Columbus, syncs with keyboard shortcuts via morphComplete, TypeScript compiles. |
| Code Quality | PASS | Clean, well-structured component. Good separation of concerns — uses existing `hideAllBuildings`/`restoreBuildings` from engine.ts. Polling pattern for viewer availability matches codebase conventions. Data-driven MODES array avoids repetition. |
| Type Safety | PASS | No new type errors introduced. Pre-existing error in `satellite/orbit-worker.ts` (DedicatedWorkerGlobalScope) is unrelated. The `(viewer.scene as any)[morph]` cast is a minor pragmatic choice to avoid verbose Cesium type gymnastics. |
| Performance | PASS | No hot-path allocations. Polling stops once viewer is found. Event listener is properly cleaned up. No render-loop work — only responds to discrete morphComplete events. |
| Patterns | PASS | Follows existing codebase patterns: inline CSSProperties, same backdrop-filter/blur dark theme, same polling-for-viewer pattern used elsewhere, same `getViewer()` import from engine.ts. Correctly placed in `src/ui/`. |

## Issues Found

1. **Minor: `as const` on MODES array prevents type narrowing of `morph` field to string literal union.** The `morph` property is typed as a wide string literal union (`'morphToColumbusView' | 'morphTo2D' | 'morphTo3D'`) by `as const`, but is then used as `(viewer.scene as any)[morph]` anyway. The `as const` is still good for documentation but the `any` cast negates the type safety benefit. Low impact — the morph method names are hardcoded and correct.

2. **Minor: CSS transition instead of spring physics.** CLAUDE.md states "All animations use spring physics, not CSS easing." However, all existing UI components (GlobalControls, TimeSlider, Toolbar) use CSS transitions for button hover/state changes. The ViewModeSwitcher's `transition: 'background 0.15s, color 0.15s'` is consistent with actual codebase practice. The spring physics convention appears to apply to globe/data animations, not UI button states.

3. **Nit: `borderRight: isLast ? 'none' : ...` on line 92.** The base `buttonStyle` already sets `border: 'none'`, so the `borderRight` override on non-last buttons adds a border that was previously removed. This works correctly but is slightly confusing — the `border: 'none'` resets all borders, then `borderRight` adds back the divider. Clear enough in context.

## Verdict
**PASS**
Clean, well-integrated component that meets all success criteria. Follows existing codebase patterns faithfully and correctly manages Cesium scene mode transitions with proper building visibility handling. No bugs or performance concerns.
