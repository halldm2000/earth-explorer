# Auto-Task Review: GeoJSON Entity Clustering

Date: 2026-03-23T08:44:14
Branch: HEAD (detached)

## Task

Add entity clustering to all GeoJSON DataSources in the layer manager (`src/features/layers/manager.ts`). When a GeoJSON DataSource is loaded, enable EntityCluster with pixelRange=45, minimumClusterSize=3. Style cluster labels to show the count. This fixes the ship tracker UI lockup when thousands of AIS points are on screen. Also add a command `layers:set-clustering` that toggles clustering on/off for a given layer.

## Changes

| File | Description |
|------|-------------|
| `src/features/layers/types.ts` | Added `clusteringEnabled?: boolean` to `LiveLayer` interface |
| `src/features/layers/manager.ts` | Configured EntityCluster after GeoJSON load in `loadGeoJson()`; added `setLayerClustering()` export; styled cluster labels via `clusterEvent` |
| `src/features/layers/commands.ts` | Added `layers:set-clustering` command with enable/disable/toggle support; added clustering status to `layers:properties` output |

## Scorecard

| Category | Score | Notes |
|----------|-------|-------|
| Completeness | **PASS** | All four success criteria met: clustering enabled on load, count labels styled, command registered and functional, type extended cleanly |
| Code Quality | **PASS WITH NOTES** | Follows existing patterns well. Two minor observations: (1) magic numbers 45/3 for pixelRange/minimumClusterSize are inline rather than named constants, though this is consistent with the codebase style; (2) `setLayerClustering` doesn't check `layer.def.kind === 'geojson'` — it only checks for `layer.datasource`, which could theoretically allow toggling on a future non-GeoJSON datasource, though the command handler guards this |
| Type Safety | **PASS** | `clusteringEnabled` is optional boolean on `LiveLayer`, consistent with the existing optional fields pattern. All usages are properly typed. (Note: tsc could not be run due to sandbox env issue — manual inspection shows no type errors introduced) |
| Performance | **PASS** | This is specifically a performance fix. Clustering with pixelRange=45 will dramatically reduce draw calls for dense point layers. The `clusterEvent` listener allocates no objects per frame — it only fires when clusters change. No render-loop impact |
| Patterns | **PASS** | Matches existing codebase patterns exactly: command structure mirrors other layer commands (same resolve/intent/handler pattern), manager function signature matches `setLayerProperty`/`setGeoJsonProperty`, UI notification via `notifyUI()` |

## Issues Found

1. **Minor — Missing re-export in `index.ts`**: `setLayerClustering` is exported from `manager.ts` but not re-exported from `src/features/layers/index.ts`. This doesn't break anything today since `commands.ts` imports directly from `./manager`, but it breaks the public API convention established by the other `setLayer*` functions that are all re-exported.

2. **Minor — No datasource kind guard in `setLayerClustering()`**: The function at `manager.ts:544` checks `!layer.datasource` but not `layer.def.kind !== 'geojson'`. The command handler (`commands.ts:575`) does guard on kind, so this is defense-in-depth only — not a bug today.

3. **Cosmetic — `clusterEvent` callback type**: The callback at `manager.ts:620` uses an inline type annotation for the `cluster` parameter rather than Cesium's own type. This works but is fragile if Cesium changes the cluster event signature. Consider using `Cesium.EntityCluster.newClusterCallback` if available.

4. **Note — PLAN.md left in repo**: The implementation plan file `PLAN.md` is still present in the workspace root. Should be removed or moved to `scripts/reports/` if needed for records.

## Verdict

**PASS WITH NOTES**

Clean, well-structured implementation that follows existing codebase patterns. The clustering configuration directly addresses the ship tracker lockup. The `layers:set-clustering` command integrates naturally with the existing command system. Only minor issues: missing re-export in `index.ts` (issue #1) and leftover `PLAN.md`. No bugs, no performance concerns, no type errors.
